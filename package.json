{
  "name": "buero.de",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm --prefix buero-backend-api run start:dev\" \"npm --prefix buero-frontend run dev\"",
    "dev:backend": "npm --prefix buero-backend-api run start:dev",
    "dev:frontend": "npm --prefix buero-frontend run dev",
    "install:all": "npm --prefix buero-backend-api install && npm --prefix buero-frontend install",
    "lint": "npm --prefix buero-frontend run lint",
    "format": "npm --prefix buero-frontend run format"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
