# Local development — backend on localhost:3000
VITE_API_URL=http://localhost:3000/api

# Production (Render Static Site build env only — do not use locally):
# VITE_API_URL=https://buro-de.onrender.com/api
